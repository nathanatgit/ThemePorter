<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>
        <!-- Debug builds only: runs the whole pipeline over adb, see DebugBuildActivity. -->
        <activity
            android:name=".debug.DebugBuildActivity"
            android:exported="true" />
        <!-- Debug builds only: switches the applied theme's icon shape over adb, see DebugShapeReceiver. -->
        <receiver
            android:name=".debug.DebugShapeReceiver"
            android:exported="true" />
    </application>

</manifest>
